pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                if [[ "${var}" == "v1" ]]
                then
                adausdasiudashdiusahdi
                else
                echo "${var}"
                fi
            }
        }
    }
}
