pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                script {
                    if (env.var == 'v1') {
                        easdsadas
                    } else {
                        echo "${var}"
                    }
                }
          
            }
        }
    }
}
